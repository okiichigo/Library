/*const { ObjectId } = require('mongodb')
const connection2 = require('./mongodata')
const jwt = require('jsonwebtoken')
require('dotenv').config()

interface User {
    _id: String
    roleid: Number
}

interface Book {
    title: String
    writer: String
    categoryId: String
}

interface CreateBookBody extends Express.Request {
    body: Book
}
:*/